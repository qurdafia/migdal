services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=migdal_prod
      - POSTGRES_USER=migdal_user
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    env_file:
      - .env.prod
    restart: always

  # 🆕 THE MESSAGE BROKER
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"

  web:
    build:
      context: ./migdalproject/migdal_platform
      dockerfile: Dockerfile
    command: /app/entrypoint.sh
    volumes:
      - static_volume:/app/static
      - media_volume:/app/media
    env_file:
      - .env.prod
    depends_on:
      - db
      - redis
    restart: always

  # 🆕 THE BACKGROUND WORKER (Executes the Playbooks)
  celery_worker:
    build:
      context: ./migdalproject/migdal_platform
      dockerfile: Dockerfile
    # 👇 FIX 1: Changed migdal_platform to migdal
    command: celery -A migdal worker -l info
    env_file:
      - .env.prod
    depends_on:
      - db
      - redis
    restart: always

  # 🆕 THE SCHEDULER (Watches the Cron Strings)
  celery_beat:
    build:
      context: ./migdalproject/migdal_platform
      dockerfile: Dockerfile
    # 👇 FIX 2: Changed to migdal AND added the DatabaseScheduler flag!
    command: celery -A migdal beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    env_file:
      - .env.prod
    depends_on:
      - db
      - redis
    restart: always

  nginx:
    build:
      context: ./migdalproject/migdal_frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - static_volume:/usr/share/nginx/html/static:ro
      - media_volume:/usr/share/nginx/html/media:ro
    depends_on:
      - web
    restart: always

volumes:
  postgres_data:
  static_volume:
  media_volume: